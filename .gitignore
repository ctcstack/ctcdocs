
# Node tooling (changesets lives at the repository root)
node_modules/
.astro/
dist/
!docs/fixtures/phase-minus-1/cloudflare-staging/dist/
!docs/fixtures/phase-minus-1/cloudflare-staging/dist/**
coverage/

# Versioned Git hook definitions (Husky runtime files remain ignored)
!.husky/
!.husky/pre-commit
!.husky/pre-push

# Working state written by the changesets release action
.changeset/prerelease.json

# Environment
# Two local credential files exist and both must stay untracked: the
# repository-root .env, which the sync CLI and the Access smoke scripts read,
# and apps/wiki/.env, which Wrangler loads from its own package directory.
.env
apps/wiki/.env
.env.local
.env.*.local

# IDE
.idea/
.vscode/

# Claude Code: shared project configuration is versioned, local overrides are not
.claude/settings.local.json
.claude/*.local.json

# OS
.DS_Store

# Logs
*.log
.codex-log/

# Test and build artifacts
playwright-report/
test-results/
blob-report/
.playwright-browsers/
.tmp/
.wrangler/

# Temporary Google credentials generated by CI auth actions
gha-creds-*.json

# Reference projects (study-only clones)
ref-projects/

# Local state written by the "impeccable" design tooling
.impeccable/
