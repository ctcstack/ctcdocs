# Node tooling
node_modules/
.astro/
coverage/

# Build output is produced by `prepack` and travels in the published tarball.
# It was committed while projects installed the packages from Git, because such
# an install runs no build script; the first npm release ended that.
dist/

# Versioned Git hook definitions (Husky runtime files remain ignored)
!.husky/
!.husky/pre-commit

# Environment
.env
.env.local
.env.*.local

# IDE
.idea/
.vscode/

# Claude Code: shared project configuration is versioned, local overrides are not
.claude/settings.local.json
.claude/*.local.json

# OS
.DS_Store

# Logs
*.log

# Test and build artifacts
playwright-report/
test-results/
blob-report/
.playwright-browsers/
.tmp/
.wrangler/

# Temporary Google credentials generated by CI auth actions
gha-creds-*.json

# Local state written by the "impeccable" design tooling
.impeccable/

# Compiled Node entrypoints of the site package
dist-node/
